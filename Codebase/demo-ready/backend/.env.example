# Backend server configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=5001
API_PREFIX=/api

# Comma-separated list of allowed web origins for CORS
# Default covers Vite dev server on common hosts
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: override the GeoTIFF dataset root (defaults to backend/datasets)
# ICE_DATASET_DIR=/absolute/path/to/datasets
